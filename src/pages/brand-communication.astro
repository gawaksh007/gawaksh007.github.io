---
import MainLayout from "../layouts/MainLayout.astro";
import { Image } from "astro:assets";
import Section from "../components/sections/Section.astro";
import SectionTitle from "../components/ui/SectionTitle.astro";
import Form from "../components/ui/Form.astro";

// Update hero images
const heroImages = [
  {
    url: "https://images.unsplash.com/photo-1552664730-d307ca884978?auto=format&fit=crop&q=80",
    alt: "Brand Strategy"
  },
  {
    url: "https://images.unsplash.com/photo-1542744173-8e7e53415bb0?auto=format&fit=crop&q=80",
    alt: "Team Collaboration"
  },
  {
    url: "https://images.unsplash.com/photo-1531973576160-7125cd663d86?auto=format&fit=crop&q=80",
    alt: "Creative Process"
  }
];

const successStories = [
  {
    title: "Global Tech Startup",
    description: "50% increase in brand recognition",
    image: "https://images.unsplash.com/photo-1460925895917-afdab827c52f?auto=format&fit=crop&q=80"
  },
  {
    title: "Retail Innovation",
    description: "2x growth in customer engagement",
    image: "https://images.unsplash.com/photo-1556742031-c6961e8560b0?auto=format&fit=crop&q=80"
  }
];

const services = [
  {
    title: "Brand Strategy",
    description: "Crafting messaging that speaks to your audience",
    icon: "📊"
  },
  {
    title: "Visual Design",
    description: "Building a stunning and consistent visual identity",
    icon: "🎨"
  },
  {
    title: "Content Marketing",
    description: "Creating content that informs and converts",
    icon: "✍️"
  },
  {
    title: "Social Media",
    description: "Amplifying your message on every platform",
    icon: "🌐"
  }
];

const processSteps = [
  {
    title: "Understand Your Audience",
    description: "Deep insights into who your customers are",
    image: "https://images.unsplash.com/photo-1454165804606-c3d57bc86b40?auto=format&fit=crop&q=80"
  },
  {
    title: "Develop Strategy",
    description: "Crafting your unique message",
    image: "https://images.unsplash.com/photo-1531538606174-0f90ff5dce83?auto=format&fit=crop&q=80"
  },
  {
    title: "Design Identity",
    description: "Creating your visual language",
    image: "https://images.unsplash.com/photo-1512758017271-d7b84c2113f1?auto=format&fit=crop&q=80"
  },
  {
    title: "Measure & Refine",
    description: "Continuous improvement",
    image: "https://images.unsplash.com/photo-1460925895917-afdab827c52f?auto=format&fit=crop&q=80"
  }
];
---

<MainLayout title="Brand Communication Services - Tagorithm">
  <main class="space-y-20">
    <!-- Hero Section with Animated Gradient -->
    <section class="relative min-h-[90vh] flex items-center overflow-hidden">
      <div class="absolute inset-0 bg-gradient-to-br from-[#B9FF66] via-white to-[#66FFB9] animate-gradient-x"></div>
      <div class="container mx-auto px-4 relative z-10">
        <div class="flex flex-col md:flex-row items-center justify-between gap-12">
          <div class="md:w-1/2 space-y-8">
            <h1 class="text-6xl font-bold leading-tight animate-fade-in">
              Transform Your <span class="text-[#B9FF66]">Brand Voice</span>
            </h1>
            <p class="text-xl text-gray-700 animate-fade-in-delay">
              We craft compelling narratives that resonate with your audience and drive meaningful engagement.
            </p>
            <div class="flex gap-4 animate-fade-in-delay-2">
              <a href="#contact" class="btn-primary">
                Start Your Journey
              </a>
              <a href="#process" class="btn-secondary">
                Learn More
              </a>
            </div>
          </div>
          <div class="md:w-1/2 relative group h-[500px] perspective-1000">
            {heroImages.map((image, index) => (
              <div 
                class={`absolute inset-0 transition-all duration-500 ease-out
                  ${index === 0 ? 'group-hover:translate-x-32 group-hover:translate-y-8 group-hover:rotate-12' : ''}
                  ${index === 1 ? 'translate-x-4 translate-y-4 group-hover:translate-x-16 group-hover:translate-y-4 group-hover:rotate-6' : ''}
                  ${index === 2 ? 'translate-x-8 translate-y-8 group-hover:translate-x-0 group-hover:translate-y-0 group-hover:rotate-0' : ''}
                `}
                style={`z-index: ${3 - index}`}
              >
                <img 
                  src={image.url}
                  alt={image.alt}
                  class="w-full h-full object-cover rounded-3xl shadow-2xl"
                />
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- Introduction Section -->
    <Section>
      <div class="container mx-auto px-4 py-20">
        <div class="grid md:grid-cols-2 gap-12 items-center">
          <div class="space-y-6">
            <SectionTitle>What is Brand Communication?</SectionTitle>
            <p class="text-xl text-gray-700">
              Brand Communication is the art of building meaningful relationships between your business and its audience. At Tagorithm, we ensure every interaction tells your story and strengthens your brand identity.
            </p>
          </div>
          <div class="grid grid-cols-2 gap-8">
            {services.map(service => (
              <div class="text-center p-6 bg-white rounded-xl shadow-lg hover:shadow-xl transition-shadow">
                <div class="text-4xl mb-4">{service.icon}</div>
                <h3 class="font-bold mb-2">{service.title}</h3>
                <p class="text-gray-600 text-sm">{service.description}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </Section>

    <!-- Services Overview -->
    <Section>
      <div class="container mx-auto px-4">
        <SectionTitle 
          sectionTitle="Our Brand Communication Services" 
          description="Comprehensive solutions to elevate your brand presence"
        />
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-12">
          <div class="p-6 bg-white rounded-xl shadow-lg hover:shadow-xl transition-shadow">
            <h3 class="text-xl font-bold mb-4">Brand Strategy</h3>
            <p class="text-gray-600">Develop a clear, compelling brand positioning that sets you apart from competitors.</p>
          </div>
          <div class="p-6 bg-white rounded-xl shadow-lg hover:shadow-xl transition-shadow">
            <h3 class="text-xl font-bold mb-4">Visual Identity</h3>
            <p class="text-gray-600">Create consistent, memorable visual elements that reflect your brand essence.</p>
          </div>
          <div class="p-6 bg-white rounded-xl shadow-lg hover:shadow-xl transition-shadow">
            <h3 class="text-xl font-bold mb-4">Brand Messaging</h3>
            <p class="text-gray-600">Craft clear, compelling messages that resonate with your target audience.</p>
          </div>
        </div>
      </div>
    </Section>

    <!-- Process Section -->
    <Section class="bg-gray-50">
      <div class="container mx-auto px-4 py-20">
        <SectionTitle>Our Approach: Data-Driven & Creative</SectionTitle>
        <div class="grid md:grid-cols-4 gap-8 mt-12">
          {processSteps.map((step, index) => (
            <div class="bg-white p-6 rounded-xl shadow-lg hover:shadow-xl transition-all transform hover:-translate-y-2">
              <img src={step.image} alt={step.title} class="w-full h-48 object-cover rounded-lg mb-6"/>
              <h3 class="text-xl font-bold mb-2">{step.title}</h3>
              <p class="text-gray-600">{step.description}</p>
            </div>
          ))}
        </div>
      </div>
    </Section>

    <!-- Success Stories -->
    <Section>
      <div class="container mx-auto px-4 py-20">
        <SectionTitle>Stories of Brands We've Transformed</SectionTitle>
        <div class="grid md:grid-cols-2 gap-8 mt-12">
          {successStories.map(story => (
            <div class="relative group overflow-hidden rounded-2xl">
              <img src={story.image} alt={story.title} class="w-full h-[400px] object-cover transition-transform group-hover:scale-110"/>
              <div class="absolute inset-0 bg-black/60 flex items-end p-8">
                <div class="text-white">
                  <h3 class="text-2xl font-bold mb-2">{story.title}</h3>
                  <p>{story.description}</p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </Section>

    <!-- Contact CTA -->
    <Section id="contact" class="bg-black text-white">
      <div class="container mx-auto px-4 py-20 text-center">
        <h2 class="text-4xl font-bold mb-8">Ready to Transform Your Brand?</h2>
        <p class="text-xl mb-12 text-gray-300">Let's create something amazing together</p>
        <a href="/contact" class="btn-primary bg-[#B9FF66] text-black hover:bg-[#A8EE55]">
          Get Started
        </a>
      </div>
    </Section>

    <!-- CTA Section -->
    <Section class="bg-black text-white">
      <div class="container mx-auto px-4 py-20 text-center">
        <h2 class="text-4xl font-bold mb-8">Let's Bring Your Brand to Life</h2>
        <p class="text-xl mb-12">Ready to elevate your brand communication?</p>
        <Form />
      </div>
    </Section>
  </main>
</MainLayout>

<style>
  .animate-gradient-x {
    animation: gradient-x 15s ease infinite;
    background-size: 400% 400%;
  }

  @keyframes gradient-x {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .animate-fade-in {
    animation: fade-in 1s ease-out;
  }

  .animate-fade-in-delay {
    animation: fade-in 1s ease-out 0.3s forwards;
    opacity: 0;
  }

  .animate-fade-in-delay-2 {
    animation: fade-in 1s ease-out 0.6s forwards;
    opacity: 0;
  }

  @keyframes fade-in {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .perspective-1000 {
    perspective: 1000px;
  }
</style>